# Public domain Caddy serves Super Productivity on (used by the caddy label).
SUPER_PRODUCTIVITY_DOMAIN=tasks.example.com

# Shared Caddy ingress network (must match the caddy-docker-proxy stack).
CADDY_INGRESS_NETWORK=caddy

# Image tag (default: latest)
SUPER_PRODUCTIVITY_TAG=latest
