# Public hostname for the read-only backup console (Authentik forward auth).
BACKUP_CONSOLE_DOMAIN=backup.example.com
CADDY_INGRESS_NETWORK=caddy

# Host paths shared with the backup scripts.
BACKUP_TMUX_DIR=/home/alien/lab/deployments/.tmux
BACKUP_LOG_FILE=/home/alien/lab/deployments/backup.log
